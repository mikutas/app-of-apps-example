apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo-apps.fullname" . }}-test-connection"
  labels:
    {{- include "foo-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foo-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
