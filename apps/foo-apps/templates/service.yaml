apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo-apps.fullname" . }}
  labels:
    {{- include "foo-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foo-apps.selectorLabels" . | nindent 4 }}
